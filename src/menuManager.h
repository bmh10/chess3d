#ifndef _MENU_MANAGER_H

#define _MENU_MANAGER_H

class MenuManager
{
  private:

  public:
    MenuManager();
    ~MenuManager();

    void Init();
    void KeyboardPress(unsigned char key);

};

#endif
